pipeline {
   agent any

   stages {
      stage('build') {
         steps {
            echo 'start to build'
         }
      }
      stage('test') {
         steps {
            echo 'start to test'
         }
      }
      stage('deploy') {
         steps {
            echo 'start to deploy'
         }
      }
   }
}

